import React from 'react'

//each category should be redirected to another page and render its own list of workouts 

function ListContainer({  }) {
  //map thru a category's workouts and 
  return (
    <div>
      
    </div>
  )
}

export default ListContainer;
